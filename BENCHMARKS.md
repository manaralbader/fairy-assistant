# Benchmarks

> Simulated backends by default (ADR 002) — see [EVALUATION_REPORT.md](EVALUATION_REPORT.md)
> for what that means. Every number below is regenerated by the script named
> next to it.

## Module 2 — provider comparison (`scripts/compare_models.py`)

| Tier | Pass rate (golden set) | Cost/conversation | Avg latency | Throughput |
|---|---|---|---|---|
| commercial | 100% | $0.000030 | 338.9ms | 10,623 req/hr |
| open_weight | 100% | $0.000006 | 166.0ms | 21,689 req/hr |

**Self-host break-even:** at a $0.50/hr fixed self-hosting cost, the
open-weight tier pays for itself above ~16,545 conversations/hour, against
measured throughput of ~21,689 req/hr at this tier's latency — i.e. it clears
break-even under the load this tier was actually measured at. Recorded in
[DECISIONS.md](DECISIONS.md).

## Module 3 — structured-output pass rates (`scripts/measure_tool_extraction.py`)

| Tier | English | Arabic |
|---|---|---|
| commercial | 89% first-attempt (no change after repair) | 89% (no change) |
| open_weight | 56% → 89% after repair | 78% → 89% after repair |

The one case per language that never passes, even after repair, is a
deliberately over-300-character inspiration note — proof the
over-specification guard is a hard schema limit, not a hint a retry can
route around.

## Module 4 — guardrail rates (`scripts/run_guard_eval.py`)

| | Attack corpus (32, bilingual) | Legitimate corpus (32, with traps) |
|---|---|---|
| Rate | **100% blocked** | **0% false positives** |

Always reported as a pair — see [EVALUATION_REPORT.md](EVALUATION_REPORT.md)
for the two deliberately hard cases (zero-width obfuscation, Arabic
authority-claim override) included in the 32.

## Module 6 — cost and latency before/after (`scripts/measure_cost.py`)

Repeated FAQ traffic (36 calls: 6 recurring questions × 6 repeats, 2 of them
Arabic), no caching vs. prompt-cache + response-cache both on:

| | Model calls | Total cost | Cache hit rate |
|---|---|---|---|
| before | 36 | $0.001875 | 95.3% (prompt-cache only) |
| after | 6 | $0.000341 | 81.7% |

**Cost reduction: 81.8%** — eval verdict: golden-set pass rate unaffected
(cached responses are exact- or near-duplicate-matched answers already
validated on their first pass through the guard, never re-guarded
differently). Near-miss suite (`scripts/measure_cost.py::NEAR_MISS_SUITE`):
**0 wrong hits** at the measured semantic threshold of 0.6.
